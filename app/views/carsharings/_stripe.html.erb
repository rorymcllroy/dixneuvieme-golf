<%= form_tag new_carsharing_passenger_path(carsharing.id) do %>
    <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
        data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
        data-image=""
        data-name= "<%= carsharing.golf.name %>"
        data-description="Paiement du covoiturage"
        data-amount="<%= (carsharing.price)*100 %>"
        data-currency="eur"
        data-email="<%= current_user.email %>"
        data-locale="auto">
    </script>
<% end %>