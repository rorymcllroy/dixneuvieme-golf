Bonjour <%= @pm.recipient.first_name %>,
==========================================================
 
Tu as reçu un message de la part de <%= @pm.sender.first_name %> <%= @pm.sender.last_name %>.
Contenu du message: <%= @pm.content %>
Date d'envoi: <%= @pm.created_at.day < 10 ? "0#{@pm.created_at.day}" : "#{@pm.created_at.day}"%>/<%=@pm.created_at.month < 10 ? "0#{@pm.created_at.month}" : "#{@pm.created_at.month}"%>/<%=@pm.created_at.year%> à <%= @pm.created_at.hour %>h<%= @pm.created_at.min < 10 ? "0#{@pm.created_at.min}": "#{@pm.created_at.min}" %>

19ème trou est une application s'enrichissant constamment des retours utilisateurs. N'hésite pas à nous faire part de tes remarques à l'adresse suivante: dixneuvieme.golf@gmail.com
 
À très vite sur les greens de France,
L'équipe 19ème trou