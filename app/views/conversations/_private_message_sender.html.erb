<blockquote class="boxed sender">
  <p><%= pm.content %> </p>
  <footer class="blockquote-footer name"> <%= pm.sender.first_name %> - <%= pm.created_at.day < 10 ? "0#{pm.created_at.day}" : "#{pm.created_at.day}" %>/<%= pm.created_at.month < 10 ? "0#{pm.created_at.month}" : "#{pm.created_at.month}" %>/<%= pm.created_at.year %> <%= pm.created_at.hour %>h<%= pm.created_at.min < 10 ? "0#{pm.created_at.min}" : "#{pm.created_at.min}" %> </footer>
</blockquote>
