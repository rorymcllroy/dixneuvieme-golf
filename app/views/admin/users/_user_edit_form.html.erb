<div class='bg-gradient'>
  <div class="container">
    <div class="row" style="padding-top:0;">
      <div class="col">
        <%= form_tag url_for(action: 'update'), method: "patch", multipart: true do %>

          <div class="form-row mb-1 flex-center">
            <div class="col-md-7">

            <%= label_tag 'Prénom*' %>
            <%= text_field_tag 'name', @user.first_name, class: 'form-control' %>

            <%= label_tag 'Nom de famille*' %>
            <%= text_field_tag 'name', @user.last_name, class: 'form-control' %>

            <%= label_tag 'Est administrateur ?*' %>
            <%= select_tag(:is_admin, options_for_select([['Oui', true], ['Non', nil]])) %>
            </div>
          </div>
          <div class="form-row mt-12">
            <div class='col flex-center'>
              <%= submit_tag "Modifier",  class: "btn btn-primary px-5" %>
            </div> 
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
