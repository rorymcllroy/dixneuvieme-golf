</br></br>
<div class="card-columns border-warning container ">
 <% @carsharings.each do |carsharing| %>
   <div class="card">
     <div class="card-header text-center">
       <h5 class="card-title text-danger"><%= carsharing.id %></h5>
     </div>
      <div class="card-body text-center">
       <p class="card-subtitle mb-3"><%= carsharing.description %></p>
       <%= link_to "Afficher", admin_carsharing_path(carsharing.id),:class => "btn-outline-warning btn p-1"%>
       <%= link_to "Modifier", edit_admin_carsharing_path(carsharing.id),:class => "btn-outline-warning btn p-1"%>
       <a href="/admin/carsharings/<%= carsharing.id %>" class="btn btn-outline-danger p-1" data-confirm="Tu es sûr?" data-method="delete">Supprimer</a>
      </div>
   </div>
 <% end %>
</div>

<div class="content row justify-content-md-center">
  <div class="card col-lg-10">
    <div class="card-body">
      <h1><%= @carsharings.count %> Covoiturages</h1>
    <div class="table-responsive">
            <table class="table table-striped table-sm">
              <thead>
                <tr>
                  <th>#</th>
                  <th>Prix</th>
                  <th>Description</th>
                  <th>Golf</th>
                  <th>Places disponibles</th>
                  <th>Conducteur</th>
                  <th>Validé</th>
                </tr>
              </thead>
              <tbody>
                <% @carsharings.each do |i| %>
                  <tr>
                    <td><%= i.id %></td>
                    <td>
                      
                    </td>
                    <td><%= i.price %></td>
                    <td> <%= i.description %></td>
                    <td><%= i.golf_id %></td>
                    <td><%= i.places %></td>
                    <td><%= i.driver_id %></td>
                    <td>
                      <%= link_to '<i class="fas fa-eye"></i>'.html_safe, admin_carsharings_path(i.id), method: "get", class:""  %>
                      <%= link_to '<i class="fas fa-pencil-alt"></i>'.html_safe, edit_admin_carsharing_path(i.id), method: "get", class:""  %>

                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>

    </div>
  </div>
</div>